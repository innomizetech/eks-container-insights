apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "amazon-cloudwatch.fullname" . }}-test-connection"
  labels:
    {{- include "amazon-cloudwatch.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "amazon-cloudwatch.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
